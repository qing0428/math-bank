services:
  app:
    build: .
    container_name: mathbank
    ports:
      - "3080:3001"
    volumes:
      - mathbank-data:/data
    environment:
      - DB_PATH=/data/mathbank.db
      - PORT=3001
    restart: unless-stopped

volumes:
  mathbank-data:
