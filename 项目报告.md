# MathBank 数学题库系统 — 项目技术报告

## 一、项目概述

MathBank 是一款面向数学教育场景的智能题库管理系统，基于多模态大语言模型（LLM）构建，实现从纸质试卷到结构化数字题库的全自动转换。系统支持图片识别、LaTeX 公式渲染、智能标签生成、分年级解题推理等核心能力，覆盖小学一年级至高三全学段。

## 二、核心痛点

传统数学题库建设面临三大瓶颈：

1. **录入成本极高**：数学题目包含大量公式、图形、特殊符号，纯手工输入一道题平均耗时 5-10 分钟，批量录入整张试卷更是耗时数小时，严重制约题库规模化建设。

2. **格式标准缺失**：不同教师录入的题目格式不统一——有人用 Word 公式编辑器，有人手写拍照，有人纯文本描述。缺乏标准化的 LaTeX 格式，导致题目无法复用、无法检索、无法跨平台迁移。

3. **解题适配困难**：同一道题对小学三年级和高中三年级学生的解法截然不同。传统系统无法根据学生年级自动调整解题策略和知识深度，导致生成的解答"对牛弹琴"。

## 三、系统架构与核心逻辑流

### 3.1 多模态识别流水线（长链推理）

系统采用**两阶段长链推理架构**完成图片到结构化数据的转换：

- **第一阶段 — 内容描述**：多模态 LLM 接收原始试卷图片，输出完整的文字描述（包含公式结构、题号、选项、图形标注等），此阶段不要求格式规范，重点确保信息完整性。

- **第二阶段 — 结构化转换**：将第一阶段的自然语言描述输入文本 LLM，转换为标准 JSON 结构，包含 LaTeX 公式、年级、知识板块、标签等字段。两阶段分离设计有效降低了单步推理的复杂度，显著提升识别准确率。

对于**批量录入**（整张试卷），系统将多张图片一次性发送给 LLM，要求按题号顺序提取全部题目，返回结构化数组，实现"一拍即得"的批量入库体验。

### 3.2 多 API 协作架构

系统设计了**双通道独立 API 配置**：

| 通道 | 用途 | 模型要求 |
|------|------|---------|
| 视觉识别 API | 图片 → LaTeX 识别 | 多模态模型（如 GPT-4o、Qwen-VL） |
| 文本生成 API | 解题推理 & 自动打标 | 纯文本模型即可 |

两条通道可独立配置不同服务商，用户可根据成本和性能灵活组合（如视觉用阿里云百炼，文本用 DeepSeek）。系统自动检测 API 兼容性（OpenAI 兼容 / Dashscope 原生），适配国内外主流 LLM 平台。

### 3.3 年级自适应推理

生成解题解析时，系统根据题目关联的年级信息动态调整 Prompt：

- **小学阶段**：限定使用算术方法、画图法，避免方程
- **初中阶段**：开放方程、不等式、平面几何定理
- **高中阶段**：允许函数、向量、导数、数列等高级工具

同时要求 LLM 列出**多种解题方法**（如代数法、几何法、数形结合法），培养学生多角度思考能力。

### 3.4 混合内容渲染引擎

系统自研了**混合内容解析器**（MixedContent），支持在同一段文本中交替渲染中文文字与 LaTeX 公式。解析器基于索引扫描而非正则匹配，通过逐字符遍历 `$` 和 `$$` 分隔符实现精准切分，配合 KaTeX 引擎完成公式渲染。对于含图形的题目，系统保留原始图片并以 1/4 栏宽并排展示，确保图文混排的完整性。

## 四、技术选型

| 层次 | 技术 |
|------|------|
| 前端框架 | React 18 + Vite |
| 样式方案 | Tailwind CSS 3 |
| 公式渲染 | KaTeX + react-katex |
| 数据持久化 | 浏览器 LocalStorage + JSON 导入/导出 |
| 部署方案 | Docker + Nginx（静态资源服务） |
| API 协议 | OpenAI Chat Completions 兼容接口 |

## 五、总结

MathBank 以多模态 LLM 为核心引擎，通过两阶段长链推理、双通道 API 协作、年级自适应 Prompt 等创新设计，将传统数小时的手工录入压缩为数秒的 AI 自动识别，同时保证了公式格式标准化和解题策略个性化。系统采用纯前端架构，数据完全本地化，零服务器依赖，适合教育机构和教师个人快速部署使用。
